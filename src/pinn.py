import torch
import torch.nn as nn
import torch.autograd as autograd

def physics_informed_loss(model, inputs, boundary_inputs, boundary_outputs, rho, mu, dt):
    return None

class PINN(nn.Module):
    pass 



if __name__ == '__main__':
    net = PINN()
    